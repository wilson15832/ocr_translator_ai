<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingStart="20dp"
    android:paddingEnd="20dp">

    <TextView
        android:id="@+id/optionLabel"
        style="@style/IosRowLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1" />

    <!-- Per-row secondary action (edit a user-added model); hidden unless the caller wants it -->
    <ImageView
        android:id="@+id/optionSecondary"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/edit_model"
        android:padding="10dp"
        android:src="@drawable/ic_edit"
        android:visibility="gone"
        app:tint="?attr/colorPrimary" />

    <ImageView
        android:id="@+id/optionCheck"
        android:layout_width="16dp"
        android:layout_height="12dp"
        android:layout_marginStart="12dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_ios_check"
        android:visibility="invisible"
        app:tint="?attr/colorPrimary" />
</LinearLayout>
